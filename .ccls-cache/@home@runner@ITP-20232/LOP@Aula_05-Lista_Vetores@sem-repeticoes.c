#include <stdio.h>
int busca(int x, int v[]) {
  int i;
  for(i = 0; i < tamanho)
  return 0;
}
int main() {
  int tamanho, i, j, valor;
  int valido = scanf("%i", &tamanho);
  int vetor[tamanho];

  for (i = 0; i < tamanho; i++) {
    valido = scanf("%d", &vetor[i]);
  }
  for (j = 0; j < tamanho; j++) {
    if(vetor[j]) {
      
    }
    printf("%d ", vetor[j]);
  }
  printf("\n");
}